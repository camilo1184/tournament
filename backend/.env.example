# ================================
# EJEMPLO DE VARIABLES DE ENTORNO
# ================================
# Copia este archivo como .env y configura los valores reales
# NUNCA subas el archivo .env a Git

# ===== SERVIDOR =====
PORT=3001
NODE_ENV=development
# En producción usar: NODE_ENV=production

# ===== BASE DE DATOS =====
# Para MongoDB (recomendado para producción)
# DATABASE_URL=mongodb://localhost:27017/tournament
# DATABASE_NAME=tournament

# Para PostgreSQL
# DATABASE_URL=postgresql://user:password@localhost:5432/tournament

# ===== AUTENTICACIÓN =====
# Generar una clave secreta segura (mínimo 32 caracteres)
# Puedes usar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-this-to-a-very-long-random-secret-key-min-32-chars
JWT_EXPIRE=24h
# Tiempo de expiración del token: 15m, 1h, 24h, 7d, etc.

# ===== CORS =====
# En desarrollo permite todo, en producción especificar dominios
CORS_ORIGIN=http://localhost:3000,http://localhost:5500
# En producción: CORS_ORIGIN=https://your-domain.com,https://www.your-domain.com

# ===== RATE LIMITING =====
# Límite de requests por IP
RATE_LIMIT_WINDOW=15
# Ventana de tiempo en minutos
RATE_LIMIT_MAX=100
# Máximo de requests por ventana

# ===== ARCHIVOS Y STORAGE =====
# Tamaño máximo de archivo de logo (en bytes)
MAX_FILE_SIZE=5242880
# 5MB = 5242880 bytes

# Si usas CDN para imágenes
# CDN_URL=https://your-cdn.com
# AWS_ACCESS_KEY_ID=your-aws-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret
# AWS_BUCKET_NAME=your-bucket

# ===== LOGS =====
LOG_LEVEL=info
# Niveles: error, warn, info, debug
LOG_FILE=logs/app.log

# ===== EMAILS (opcional) =====
# Si implementas notificaciones por email
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# ===== REDIS (opcional) =====
# Para caché y sesiones
# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=your-redis-password

# ===== MONITOREO =====
# Sentry para tracking de errores
# SENTRY_DSN=https://your-sentry-dsn

# ===== BACKUPS =====
# Configuración de backups automáticos
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *
# Cron format: cada día a las 2 AM
BACKUP_RETENTION_DAYS=30
# Mantener backups por 30 días
