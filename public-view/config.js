// Configuración de la vista pública
// Para mostrar solo torneos de un usuario específico, establece USER_ID
// Puedes obtener tu USER_ID desde la consola del navegador en el panel de administración

const CONFIG = {
    // Deja vacío para mostrar todos los torneos
    // O establece tu userId para filtrar: '673ec891461ab0d73e395870'
    USER_ID: '',
    
    // URL del backend (se detecta automáticamente, pero puedes sobreescribir)
    API_BASE_URL: null
};
